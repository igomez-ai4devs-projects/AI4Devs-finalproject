---
id: T-C1-07
epic: C1
story: US-C1-01
requirements: [FR-INC-01]
shape: greenfield
platform: backend
layer: application
agent: backend-engineer
estimate: 3h
phase: disputed 0/1 (F6)
---

# [C1] T-C1-07 · `LogIncidentUseCase` for a requester, reporter taken from the session

## Context

`US-C1-01` requires the reporter to be taken from the authenticated session and **never** from a field the requester can type. `ARCHITECTURE.md` §9 places authorization in `type:application` use cases, expressed in domain terms and testable without HTTP; the resolved `Actor` is assembled per request by `C10` (`T-C10-39`) and is consumed here, not rebuilt.

`ARCHITECTURE.md` §8 shows this use case also attaching the SLA commitment through `SlaPolicyPort`. The port is declared here; the adapter that resolves it against `C7` lives in `apps/api` and is `T-C1-58`. `incident` never imports `sla`.

**Two notes from ADR-014 (`DATA-MODEL.md` §8.5).** First, this slice's use case does not assign a lifecycle state — `Incident.log()` (`T-C1-05`) has no `workflow_id`/`state_id` slot to set, because the state model does not exist until `T-C1-49`/`T-C1-50`; there is no "resting in `New`" to persist yet, only the absence of a lifecycle column. Second, whatever `SlaPolicyPort.attachFor()` eventually does with the logged Incident, it receives one with `priority: null` — Priority is not derived until block D (`T-C1-30`) — so `C7`'s policy-resolution behavior for a not-yet-prioritized ticket is that context's own open question (ADR-014 Consequences), not answered by this use case.

## Scope

- `LogIncidentUseCase` in `libs/incident/application`: authorize the actor, allocate the reference, call `Incident.log()`, save in a single transaction, publish events **after commit**.
- Reporter resolved from the `Actor`; any reporter identifier present in the command is ignored rather than trusted.
- `SlaPolicyPort` declared in `libs/incident/domain` with `attachFor()`; the use case calls it and tolerates an unbound adapter in tests through a stub.
- Post-commit publication through `EventPublisherPort`, so a failing audit or notification subscriber cannot roll back a logged Incident (ADR-008, `NFR-AVL-03`).
- Out of scope: the HTTP adapter and DTOs (`T-C1-08`); the agent-on-behalf path (`T-C1-11`); the SLA adapter (`T-C1-58`); the scope-rule evaluation at intake (`T-C1-87`).

## Acceptance criteria

**Given** an authenticated requester and a valid command **When** the use case executes **Then** the Incident is persisted with its reference, the reporter equals the session actor, and `IncidentLogged` is published exactly once after the transaction commits.

**Given** a command carrying a reporter identifier different from the session actor **When** the use case executes **Then** the persisted reporter is the session actor and the supplied value is discarded.

**Given** a subscriber that throws when handling `IncidentLogged` **When** the use case executes **Then** the Incident remains persisted and the failure is isolated to the subscriber.

**Given** the use case **When** it is unit-tested **Then** it runs against stubbed ports with no HTTP and no database.
