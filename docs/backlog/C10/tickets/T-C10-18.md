---
id: T-C10-18
epic: C10
story: —
foundation: true
requirements: []
shape: greenfield
platform: shared
layer: workspace scaffolding (5 identity-access libraries)
agent: —
estimate: 2h
phase: 0
---

# [C10] T-C10-18 · Scaffold the five `identity-access` libraries with their tags

## Context

Foundation work with no user story behind it. Source: `docs/backlog/epic-map.md` → section **`C10` · Identity & Access Management**, *What actually remains*, which states the epic stands up *and only then its own five `identity-access` libraries (`domain`, `application`, `infrastructure`, `feature`, `data-access` — no `ui` of its own per `ARCHITECTURE.md` §5.1)*; and finding **F14** of `user-stories.md`, which records that scaffolding carries no persona and no observable behavior.

The five libraries span both platforms, so the work belongs to neither dev agent alone; it sits at the **workspace scaffolding** layer on the **shared platform**, hence `agent: —`.

## Scope

- Generate exactly five libraries with the tags of `ARCHITECTURE.md` §5.5:
  - `libs/identity-access/domain` — `platform:backend,scope:identity-access,type:domain`
  - `libs/identity-access/application` — `platform:backend,scope:identity-access,type:application`
  - `libs/identity-access/infrastructure` — `platform:backend,scope:identity-access,type:infrastructure`
  - `libs/identity-access/feature` — `platform:frontend,scope:identity-access,type:feature`
  - `libs/identity-access/data-access` — `platform:frontend,scope:identity-access,type:data-access`
- Barrel `index.ts` per library; Jest configured per library.
- **No `ui` library for this context.** Its screens compose the shared design system directly (`ARCHITECTURE.md` §5.1).
- Out of scope: any type, port, use case, entity or component.

## Acceptance criteria

Mechanical checks — this ticket has no user scenario.

**Given** the workspace **When** the five projects are listed **Then** exactly those five exist under `libs/identity-access/`, and no `libs/identity-access/ui` project exists.

**Given** each project **When** its tags are inspected **Then** it carries exactly three tags matching the list above.

**Given** a probe import from `identity-access/infrastructure` into `identity-access/domain` **When** lint runs **Then** it fails on the type matrix — `type:domain` may depend only on `domain`/`util`, and this is the only direction between these two libraries that is actually illegal: `type:infrastructure` legally depends on `type:domain` (`ARCHITECTURE.md` §5.3), so a probe run the other way around would pass; the probe is reverted before the ticket is closed.

**Given** a probe import added to `identity-access/application`, of a temporary project tagged `platform:frontend,scope:identity-access,type:util` — same scope, and `util` is a `type:` `application` may legally depend on; only the platform is illegal — **When** lint runs **Then** it fails on the `platform:` rule. (A probe importing `identity-access/feature` directly does not isolate this: `type:application` may not depend on `type:feature` either, and `enforce-module-boundaries` reports only the first `depConstraints` match — the type-matrix family, checked before platform — so the platform message would never appear.) Both the import and the temporary project are removed before the ticket is closed.

**Given** the workspace **When** `pnpm nx run-many -t lint test` runs **Then** all five projects pass.
