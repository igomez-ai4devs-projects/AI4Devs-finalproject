---
id: T-C10-61
epic: C10
story: US-C10-15
requirements: [FR-IAM-06]
shape: greenfield
platform: frontend
layer: feature + data-access (identity-access)
agent: frontend-engineer
estimate: 2.5h
phase: 1
blocked_by: F16
---

# [C10] T-C10-61 · Web step-up re-authentication prompt

## Context

The client half of `T-C10-60`: `US-C10-15` requires the administrator to prove identity **for this attempt**, then have the original operation retried, this time carrying that proof, and succeed.

**Blocked by finding F16, partially resolved** — the privileged set is now the PRD's own list (`FR-IAM-06`); the remaining `NFR-SEC-06` judgment call is what keeps this ticket blocked. The prompt itself is buildable; the full membership it ultimately covers is not fixed.

**One clarification carried over from `T-C10-60`'s redesign.** The retry this prompt performs is not "the same request again, now that a session mark exists" — there is no mark anywhere. It is the **same original request, reissued with the credential just entered attached to it**. The distinction only matters at the wiring level (where the credential is attached), not to this ticket's user-facing behavior, which is unchanged.

## Scope

- Interceptor handling of the distinct re-authentication-required error code from `T-C10-60`: open the step-up prompt rather than routing to sign-in, which is what a `401` does.
- Step-up overlay built from `T-C10-14` and the form primitives of `T-C10-13`: credential entry, cancel, and a clear explanation of why it is being asked.
- On success, the original request is reissued with the entered credential attached to it, transparently, and its result is delivered to the original caller; on cancel, the operation is abandoned and the screen says so.
- Invalid credentials show an error in the prompt without closing it and without signing the user out.
- Jest tests for retry, cancel and invalid credentials.
- Out of scope: the server-side enforcement; the remaining `NFR-SEC-06` judgment call.

## Acceptance criteria

**Given** a privileged action refused for re-authentication **When** the response reaches the client **Then** the step-up prompt opens and the user is not signed out.

**Given** valid credentials in the prompt **When** they are submitted **Then** the original request is retried automatically and its result reaches the original caller.

**Given** invalid credentials in the prompt **When** they are submitted **Then** an error is shown inside the prompt, the prompt stays open, and the session is not terminated.

**Given** the prompt is cancelled **When** it closes **Then** the operation is abandoned, the screen states that nothing was changed, and focus returns to the invoking control.

**Given** a genuine `401` **When** it reaches the interceptor **Then** the client routes to sign-in and does **not** open the step-up prompt.
